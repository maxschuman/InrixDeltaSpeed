@{
    Server.ScriptTimeout = 300;
    Page.Title = "Inrix DeltaSpeed: Home";
}
<script type="text/javascript">
    function ShowProgress() {
        setTimeout(function () {
            var modal = $('<div />');
            modal.addClass("modal");
            $('body').append(modal);
            var loading = $(".loading");
            loading.show();
            var top = Math.max($(window).height() / 2 - loading[0].offsetHeight / 2, 0);
            var left = Math.max($(window).width() / 2 - loading[0].offsetWidth / 2, 0);
            loading.css({ top: top, left: left });
        }, 200);
    }
    $(document).on("click", ".other_pages", function (ev) {
        ShowProgress();
    });
</script>
<div class="loading" align="center">
    Loading. Please wait.<br />
    <br />
    <img src="ellipsis.gif" alt="" />
</div>
<div id="navbar" class="NavigationMenu">
    <ul>
        <li>
            <a class="CurrentPage" title="Home">Home</a>
        </li>
        <li>
            <a class="other_pages" href="~/Queues" title="Queues">Queues</a>
        </li>
        <li>
            <a class="other_pages" href="~/Backs" title="Backs-of-Queue">Backs-of-Queue</a>
        </li>
        <li>
            <a class="other_pages" href="~/About" title="About">About</a>
        </li>
    </ul>
</div>
<div class="MainContent">
    <div class="PanelContainer">
        <div class="ShowcasePanel">
            <p id="HomepageText">
                This website displays listings of and data about back-of-queue events and full-length queues on interstate highways and other major freeways. The following states are covered:
            </p>
            <ul>
                <li>Florida</li>
                <li>Ohio</li>
                <li>Tennessee</li>
            </ul>
        </div>
    </div>
</div>
        
